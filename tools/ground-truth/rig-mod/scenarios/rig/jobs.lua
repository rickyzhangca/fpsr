-- Placeholder overwritten by the launcher with one or more capture jobs.
-- Shape:
--   return {
--     zoom = 2,
--     jobs = {
--       { name = "smoke", blueprint = "...", show_entity_info = false },
--     },
--   }
return {
  zoom = 2,
  jobs = {},
}
