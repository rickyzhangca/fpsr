{
  "name": "@fpsr/corpus",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "tsx src/generate.ts",
    "generate:base": "tsx src/generate-base.ts",
    "generate:official-mods": "tsx src/generate-official-mods.ts",
    "check": "tsx src/check.ts",
    "build": "echo 'no build step'",
    "test": "vp test run",
    "lint": "vp lint",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.test.json --noEmit",
    "format": "vp fmt"
  },
  "dependencies": {
    "skia-canvas": "^2.0.2",
    "@rickyzhangca/fpsr": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  }
}
